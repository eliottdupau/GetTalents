<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eliottdup.gettalents">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/GetTalents">
        <activity android:name=".ui.picture.PictureActivity"></activity>
        <activity android:name=".ui.profile.edit.EditProfileActivity" />
        <activity android:name=".ui.relation.RelationActivity" />
        <activity android:name=".ui.review.ReviewListActivity" />
        <activity android:name=".ui.address.AddressActivity" />
        <activity android:name=".ui.profile.consult.other.UserProfileActivity" />
        <activity android:name=".ui.chat.ChatActivity" />
        <activity android:name=".ui.review.ReviewActivity" />
        <activity android:name=".ui.profile.consult.mine.MyProfileActivity" />
        <activity android:name=".ui.favorite.FavoriteActivity" />
        <activity android:name=".ui.authentication.AuthenticationActivity" />
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.android.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>